--[[
	Professional Athlete Career Events
	AAA-quality events with random BitLife-style outcomes
	User CANNOT control outcomes - everything is randomized
	
	Careers covered:
	- Professional Athlete
	- Football Player
	- Basketball Player
	- Baseball Player
	- Soccer Player
	- MMA Fighter
	- Olympic Athlete
	
	CRITICAL FIX #RVS-7: Premium path blocking for royalty/mafia players
]]

local AthleteCareerEvents = {}

-- ═════════════════════════════════════════════════════════════════════════════════════
-- CRITICAL FIX #RVS-ATHLETE-1: Premium path blocking
-- Athlete events should NOT fire for royalty or mafia players (unless already an athlete)
-- ═════════════════════════════════════════════════════════════════════════════════════
local function isBlockedByPremiumPath(state)
	if not state.Flags then return false end
	local flags = state.Flags
	
	-- If player is already an athlete, allow
	if flags.signed_athlete or flags.pro_athlete then
		return false
	end
	
	-- Block if player chose royalty path
	if flags.primary_wish_type == "royalty" then return true end
	if flags.is_royalty or flags.royal_birth or flags.dating_royalty then return true end
	if state.RoyalState and state.RoyalState.isRoyal then return true end
	
	-- Block if player chose mafia path
	if flags.primary_wish_type == "mafia" then return true end
	if flags.in_mob then return true end
	if state.MobState and state.MobState.inMob then return true end
	
	return false
end

-- ═══════════════════════════════════════════════════════════════════════════════
-- CRITICAL FIX #16: Enhanced helper functions for athletic careers
-- ═══════════════════════════════════════════════════════════════════════════════
local function hasAthleteJob(state)
	-- CRITICAL FIX #RVS-ATHLETE-2: Check premium path blocking first
	if isBlockedByPremiumPath(state) then return false end
	if not state.CurrentJob then return false end
	local jobId = (state.CurrentJob.id or ""):lower()
	local jobCat = (state.CurrentJob.category or ""):lower()
	-- CRITICAL FIX #17: Exclude non-athlete entertainment careers
	if jobId:find("actor") or jobId:find("singer") or jobId:find("musician")
	   or jobId:find("rapper") or jobId:find("streamer") or jobId:find("youtuber") then
		return false
	end
	return jobCat == "sports" or jobId:find("athlete") or jobId:find("player") or 
		   jobId:find("football") or jobId:find("basketball") or jobId:find("baseball") or
		   jobId:find("soccer") or jobId:find("mma") or jobId:find("fighter") or
		   jobId:find("tennis") or jobId:find("golf") or jobId:find("hockey") or
		   jobId:find("boxing") or jobId:find("racing")
end

local function isProAthlete(state)
	if not state.CurrentJob then return false end
	local jobId = (state.CurrentJob.id or ""):lower()
	return jobId:find("pro") or jobId:find("professional") or 
		   (state.Flags and state.Flags.signed_athlete)
end

-- CRITICAL FIX #18: Check for specific sport type
local function isTeamSportAthlete(state)
	if not hasAthleteJob(state) then return false end
	local jobId = (state.CurrentJob.id or ""):lower()
	return jobId:find("football") or jobId:find("basketball") or jobId:find("baseball")
		or jobId:find("soccer") or jobId:find("hockey")
end

-- CRITICAL FIX #19: Check for individual sport athlete
local function isIndividualAthlete(state)
	if not hasAthleteJob(state) then return false end
	local jobId = (state.CurrentJob.id or ""):lower()
	return jobId:find("tennis") or jobId:find("golf") or jobId:find("mma")
		or jobId:find("boxing") or jobId:find("racing") or jobId:find("swimmer")
end

-- CRITICAL FIX #20: Check if player is in prime athletic years
local function isInPrime(state)
	local age = state.Age or 25
	return age >= 22 and age <= 34
end

-- CRITICAL FIX #21: Check if athlete is aging/declining
local function isVeteranAthlete(state)
	local age = state.Age or 25
	return age >= 32 and hasAthleteJob(state)
end

AthleteCareerEvents.events = {
	-- ══════════════════════════════════════════════════════════════════════════════
	-- PROFESSIONAL ATHLETE EVENTS
	-- ══════════════════════════════════════════════════════════════════════════════
	{
		id = "athlete_big_game",
		title = "🏆 Championship Game!",
		emoji = "🏆",
		text = "It's the championship! Your team is counting on you! The whole world is watching!",
		category = "career_sports",
		weight = 18,
		minAge = 18, maxAge = 42,
		baseChance = 0.5,
		cooldown = 3,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true, injured = true },
		
		choices = {
			{
				text = "Give it everything you've got!",
				effects = { Health = -5 },
				feedText = "This is your moment!",
				triggerMinigame = "mash",
				minigameOptions = { difficulty = "hard" },
				onResolve = function(state, minigameResult)
					local roll = math.random(1, 100)
					local bonus = (minigameResult and minigameResult.success) and 25 or 0
					
					if roll + bonus >= 55 then
						local prize = math.random(500000, 3000000)
						state.Money = (state.Money or 0) + prize
						state:ModifyStat("Happiness", 30)
						state.Fame = math.min(100, (state.Fame or 0) + 60) -- CRITICAL FIX: Cap fame at 100
						state.Flags = state.Flags or {}
						state.Flags.champion_athlete = true
						state:AddFeed(string.format("🏆 CHAMPIONS! MVP PERFORMANCE! $%s bonus! Parades in your honor!", prize))
					elseif roll + bonus >= 30 then
						local prize = math.random(100000, 400000)
						state.Money = (state.Money or 0) + prize
						state:ModifyStat("Happiness", 10)
						state.Fame = (state.Fame or 0) + 20
						state:AddFeed(string.format("🏆 Lost the game but you played well. $%s consolation. Next year!", prize))
					else
						state:ModifyStat("Happiness", -20)
						state.Fame = (state.Fame or 0) - 10
						state:AddFeed("🏆 Made a crucial error that cost the game. The city hates you right now.")
					end
				end,
			},
			{
				text = "Play it safe - don't get injured",
				effects = { Health = 3 },
				feedText = "Career longevity matters...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						local prize = math.random(200000, 600000)
						state.Money = (state.Money or 0) + prize
						state:ModifyStat("Happiness", 15)
						state.Fame = (state.Fame or 0) + 25
						state:AddFeed(string.format("🏆 Team won anyway! $%s! Smart preservation paid off!", prize))
					else
						state:ModifyStat("Happiness", -15)
						state:AddFeed("🏆 Team lost. Coach blames your 'lack of effort'. Not a good look.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_injury_scare",
		title = "🏥 Injury During Practice!",
		emoji = "🏥",
		text = "You felt something pop during practice. The medical staff is concerned. MRI scheduled for tomorrow.",
		category = "career_sports",
		weight = 16,
		minAge = 18, maxAge = 45,
		baseChance = 0.55,
		cooldown = 4, -- CRITICAL FIX: Increased from 2 to reduce spam
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true },
		
		choices = {
		{
			text = "Get the full medical workup ($5K)",
			effects = { Money = -5000 },
			feedText = "Health comes first...",
			eligibility = function(state) return (state.Money or 0) >= 5000, "💸 Need $5K for medical workup" end,
			onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						state:ModifyStat("Happiness", 10)
						state:AddFeed("🏥 Just a strain! Rest for a week and you're good. Huge relief!")
					elseif roll <= 75 then
						state:ModifyStat("Happiness", -10)
						state:ModifyStat("Health", -5)
						state.Flags = state.Flags or {}
						state.Flags.minor_injury = true
						state:AddFeed("🏥 Partial tear. 6-8 weeks out. Could be worse.")
					else
						state:ModifyStat("Happiness", -25)
						state:ModifyStat("Health", -15)
						state.Flags = state.Flags or {}
						state.Flags.major_injury = true
						state:AddFeed("🏥 Major injury! Season-ending. Surgery required. Long road ahead.")
					end
				end,
			},
			{
				text = "Play through it - tough it out",
				effects = { Happiness = -3 },
				feedText = "Can't let the team down...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 30 then
						state:ModifyStat("Happiness", 8)
						state:AddFeed("🏥 It was nothing! Played great and showed mental toughness!")
					elseif roll <= 60 then
						state:ModifyStat("Happiness", -8)
						state:ModifyStat("Health", -8)
						state:AddFeed("🏥 Made it worse by playing. Now you're out even longer.")
					else
						state:ModifyStat("Happiness", -30)
						state:ModifyStat("Health", -25)
						state.Flags = state.Flags or {}
						state.Flags.career_threatening_injury = true
						state:AddFeed("🏥 CATASTROPHIC! Complete tear! Your career might be over!")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_contract_negotiation",
		title = "💰 Contract Renewal Time!",
		emoji = "💰",
		text = "Your contract is up. You've been playing well and your agent says you can demand big money!",
		category = "career_sports",
		weight = 15,
		minAge = 18, maxAge = 40,
		baseChance = 0.5,
		cooldown = 3,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Demand max money",
				effects = {},
				feedText = "I know my worth!",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						local contract = math.random(10000000, 50000000)
						state.Money = (state.Money or 0) + contract
						state:ModifyStat("Happiness", 25)
						state.Fame = (state.Fame or 0) + 15
						state.Flags = state.Flags or {}
						state.Flags.max_contract = true
						state:AddFeed(string.format("💰 GOT THE MAX! $%s contract! Set for life!", contract))
					elseif roll <= 70 then
						local contract = math.random(3000000, 8000000)
						state.Money = (state.Money or 0) + contract
						state:ModifyStat("Happiness", 10)
						state:AddFeed(string.format("💰 Didn't get max, but $%s is still great!", contract))
					else
						state:ModifyStat("Happiness", -15)
						state:AddFeed("💰 Team let you walk. Now scrambling to find a new team for less money.")
					end
				end,
			},
			{
				text = "Take a team-friendly deal",
				effects = { Happiness = 5 },
				feedText = "Want to stay with this team...",
				onResolve = function(state)
					local contract = math.random(2000000, 6000000)
					state.Money = (state.Money or 0) + contract
					state:ModifyStat("Happiness", 12)
					state.Flags = state.Flags or {}
					state.Flags.team_player = true
					state:AddFeed(string.format("💰 $%s team-friendly deal! Fans love your loyalty!", contract))
				end,
			},
			{
				text = "Test free agency",
				effects = { Happiness = -5 },
				feedText = "See what's out there...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 35 then
						local contract = math.random(15000000, 60000000)
						state.Money = (state.Money or 0) + contract
						state:ModifyStat("Happiness", 25)
						state.Fame = (state.Fame or 0) + 10
						state:AddFeed(string.format("💰 Bidding war! Got $%s from a new team!", contract))
					elseif roll <= 65 then
						local contract = math.random(5000000, 12000000)
						state.Money = (state.Money or 0) + contract
						state:ModifyStat("Happiness", 8)
						state:AddFeed(string.format("💰 Found a new home. $%s contract with a contender.", contract))
					else
						state:ModifyStat("Happiness", -20)
						state:AddFeed("💰 No good offers. Had to sign for the minimum. Humbling.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_ped_offer",
		title = "💊 PED Offer",
		emoji = "💊",
		text = "A teammate secretly offers you performance-enhancing drugs. Says half the league is using them.",
		category = "career_sports",
		weight = 12,
		minAge = 18, maxAge = 40,
		baseChance = 0.4,
		cooldown = 4,
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Take them - everyone's doing it",
				effects = { Health = -3 },
				feedText = "Leveling the playing field...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 35 then
						state:ModifyStat("Happiness", 15)
						state:ModifyStat("Health", 5)
						state.Money = (state.Money or 0) + math.random(500000, 2000000)
						state.Flags = state.Flags or {}
						state.Flags.enhanced_athlete = true
						state:AddFeed("💊 Performance through the roof! Best season ever! Nobody suspects.")
					elseif roll <= 65 then
						state:ModifyStat("Happiness", 5)
						state:AddFeed("💊 Helped a bit but not dramatically. Worth the risk?")
					else
						state:ModifyStat("Happiness", -30)
						state.Fame = (state.Fame or 0) - 50
						state.Flags = state.Flags or {}
						state.Flags.banned_athlete = true
						state.CurrentJob = nil
						state:AddFeed("💊 FAILED DRUG TEST! Suspended indefinitely! Legacy tarnished forever!")
					end
				end,
			},
			{
				text = "Refuse - not worth the risk",
				effects = { Smarts = 3, Happiness = 5 },
				setFlags = { clean_athlete = true },
				feedText = "Natural or nothing.",
				onResolve = function(state)
					state:ModifyStat("Happiness", 5)
					state:AddFeed("💊 Stayed clean. Sleep well at night knowing your achievements are real.")
				end,
			},
			{
				text = "Report to the league",
				effects = { Smarts = 2 },
				feedText = "This needs to stop...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						state:ModifyStat("Happiness", 10)
						state:AddFeed("💊 Investigation launched! You're protected as a whistleblower.")
					else
						state:ModifyStat("Happiness", -15)
						state.Flags = state.Flags or {}
						state.Flags.snitch_athlete = true
						state:AddFeed("💊 Word got out you snitched. Locker room is ICE COLD now.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_endorsement_deal",
		title = "📸 Major Endorsement Offer!",
		emoji = "📸",
		text = "A major brand wants you as their spokesperson! Commercials, shoe deals, the works!",
		category = "career_sports",
		weight = 14,
		minAge = 18, maxAge = 45,
		baseChance = 0.45,
		cooldown = 3,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Sign the deal!",
				effects = { Happiness = 15 },
				feedText = "Time to cash in on fame!",
				onResolve = function(state)
					local deal = math.random(500000, 5000000)
					state.Money = (state.Money or 0) + deal
					state.Fame = (state.Fame or 0) + 20
					state.Flags = state.Flags or {}
					state.Flags.endorsed_athlete = true
					state:AddFeed(string.format("📸 Signed! $%s endorsement deal! Your face is everywhere!", deal))
				end,
			},
			{
				text = "Negotiate for more",
				effects = {},
				feedText = "My image is worth more...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 35 then
						local deal = math.random(3000000, 15000000)
						state.Money = (state.Money or 0) + deal
						state:ModifyStat("Happiness", 20)
						state.Fame = (state.Fame or 0) + 25
						state:AddFeed(string.format("📸 They REALLY wanted you! $%s mega-deal!", deal))
					elseif roll <= 70 then
						local deal = math.random(800000, 4000000)
						state.Money = (state.Money or 0) + deal
						state:ModifyStat("Happiness", 12)
						state:AddFeed(string.format("📸 Got a bit more. $%s. Not bad.", deal))
					else
						state:ModifyStat("Happiness", -10)
						state:AddFeed("📸 They went with another athlete. Missed opportunity.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_trade_rumors",
		title = "📰 Trade Rumors!",
		emoji = "📰",
		text = "The media is reporting your team is shopping you. You might get traded to a team across the country!",
		category = "career_sports",
		weight = 13,
		minAge = 18, maxAge = 38,
		baseChance = 0.45,
		cooldown = 3,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Request a trade to a contender",
				effects = { Happiness = -5 },
				feedText = "Want to win a championship...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 50 then
						state:ModifyStat("Happiness", 15)
						state.Money = (state.Money or 0) + math.random(100000, 500000)
						state.Flags = state.Flags or {}
						state.Flags.traded_to_contender = true
						state:AddFeed("📰 Trade granted! Going to a championship team! Ring time!")
					else
						state:ModifyStat("Happiness", -10)
						state:AddFeed("📰 No contender wanted you. Stuck in a bad situation.")
					end
				end,
			},
			{
				text = "Say you want to stay",
				effects = { Happiness = 5 },
				feedText = "This is home...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 60 then
						state:ModifyStat("Happiness", 10)
						state.Flags = state.Flags or {}
						state.Flags.franchise_player = true
						state:AddFeed("📰 Front office honored your wishes! Staying put! Fans love you!")
					else
						state:ModifyStat("Happiness", -15)
						state:AddFeed("📰 Traded anyway. Your preferences didn't matter. Cold business.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_retirement_consider",
		title = "🎽 Time to Retire?",
		emoji = "🎽",
		text = "Your body is breaking down. The injuries are piling up. Maybe it's time to hang it up?",
		category = "career_sports",
		weight = 12,
		minAge = 32, maxAge = 50,
		baseChance = 0.45,
		cooldown = 4,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Retire at the top",
				effects = { Health = 10, Happiness = 10 },
				setFlags = { retired_athlete = true },
				feedText = "Going out on my terms.",
				onResolve = function(state)
					state.Fame = (state.Fame or 0) + 30
					state:AddFeed("🎽 Retired as a legend! Hall of Fame career! Analyst offers flooding in!")
				end,
			},
			{
				text = "Play one more year",
				effects = { Health = -5 },
				feedText = "Not ready to give this up...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 35 then
						state.Money = (state.Money or 0) + math.random(1000000, 5000000)
						state:ModifyStat("Happiness", 15)
						state:AddFeed("🎽 One more GREAT year! Perfect ending! Retire on top!")
					elseif roll <= 70 then
						state:ModifyStat("Happiness", -5)
						state:AddFeed("🎽 Mediocre year. Maybe should have retired earlier.")
					else
						state:ModifyStat("Happiness", -20)
						state:ModifyStat("Health", -15)
						state.Flags = state.Flags or {}
						state.Flags.forced_retirement = true
						state:AddFeed("🎽 Career-ending injury! Forced retirement! Painful way to go out.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_hall_of_fame",
		title = "🏛️ Hall of Fame Nomination!",
		emoji = "🏛️",
		text = "You've been nominated for the Hall of Fame! The voting is this week!",
		category = "career_sports",
		weight = 8,
		minAge = 35, maxAge = 80,
		baseChance = 0.35,
		cooldown = 5,
		oneTime = true,
		eligibility = function(state)
			return (state.Flags and state.Flags.retired_athlete) or 
				   (state.Flags and state.Flags.champion_athlete)
		end,
		blockedByFlags = { in_prison = true, hall_of_famer = true, banned_athlete = true },
		
		choices = {
			{
				text = "Wait and hope for the best",
				effects = { Happiness = 5 },
				feedText = "The ultimate honor...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 60 then
						state:ModifyStat("Happiness", 30)
						state.Fame = math.min(100, (state.Fame or 0) + 100) -- CRITICAL FIX: Cap fame at 100
						state.Flags = state.Flags or {}
						state.Flags.hall_of_famer = true
						state:AddFeed("🏛️ HALL OF FAME! INDUCTED! Your legacy is immortalized forever!")
					else
						state:ModifyStat("Happiness", 5)
						state.Fame = (state.Fame or 0) + 10
						state:AddFeed("🏛️ Didn't make it this year. Maybe next time. Still an honor to be nominated.")
					end
				end,
			},
		},
	},
	
	-- ═══════════════════════════════════════════════════════════════════════════════
	-- CRITICAL FIX #ATHLETE-5: ADDED MANY MORE ATHLETE EVENTS FOR VARIETY!
	-- User complaint: "Fix AthleteCareerEvents - ensure proper integration"
	-- Adding diverse events ensures athletes have engaging content throughout career
	-- ═══════════════════════════════════════════════════════════════════════════════
	
	{
		id = "athlete_rookie_hazing",
		title = "😤 Rookie Treatment",
		emoji = "😤",
		text = "The veterans are making you carry their bags, pay for dinners, and do embarrassing initiation rituals.",
		category = "career_sports",
		weight = 14,
		minAge = 18, maxAge = 24,
		baseChance = 0.5,
		cooldown = 3,
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true, veteran_athlete = true },
		
		choices = {
			{
				text = "Go along with it - pay your dues (~$5K)",
				effects = { Happiness = -5, Money = -5000 },
				feedText = "Pay your dues...",
				eligibility = function(state) return (state.Money or 0) >= 5000, "💸 Need $5K to cover rookie expenses" end,
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 70 then
						state:ModifyStat("Happiness", 10)
						state.Flags = state.Flags or {}
						state.Flags.accepted_by_team = true
						state:AddFeed("😤 Earned respect! You're officially part of the team now!")
					else
						state:AddFeed("😤 Some of this crossed the line. Uncomfortable but survived.")
					end
				end,
			},
			{
				text = "Refuse - this is harassment",
				effects = { Smarts = 2 },
				feedText = "I'm a professional, not a punching bag.",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						state:ModifyStat("Happiness", 8)
						state:AddFeed("😤 Vets respected your boundaries. Times are changing!")
					else
						state:ModifyStat("Happiness", -10)
						state.Flags = state.Flags or {}
						state.Flags.team_outcast = true
						state:AddFeed("😤 Labeled as 'soft'. Cold shoulder from the locker room.")
					end
				end,
			},
			-- CRITICAL FIX: Add a compromise option
			{
				text = "Set reasonable boundaries but be a good sport",
				effects = { Happiness = 2 },
				feedText = "😤 You did some stuff, skipped the worst parts. Found a middle ground.",
			},
		},
	},
	
	{
		id = "athlete_media_controversy",
		title = "🎤 Press Conference Gone Wrong",
		emoji = "🎤",
		text = "During a press conference, a reporter asks you a loaded question about a controversial topic. The cameras are rolling.",
		category = "career_sports",
		weight = 12,
		minAge = 18, maxAge = 45,
		baseChance = 0.45,
		cooldown = 4, -- CRITICAL FIX: Increased from 2 to reduce spam
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Speak your mind honestly",
				effects = {},
				feedText = "Here's what I really think...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 35 then
						state.Fame = (state.Fame or 0) + 25
						state:ModifyStat("Happiness", 15)
						state:AddFeed("🎤 Your authentic take went VIRAL! Respected for speaking truth!")
					elseif roll <= 70 then
						state:ModifyStat("Happiness", -5)
						state:AddFeed("🎤 Mixed reactions. Some love you, some hate you. Typical.")
					else
						state:ModifyStat("Happiness", -20)
						state.Fame = (state.Fame or 0) - 15
						state:AddFeed("🎤 BACKLASH! Sponsors are not happy. PR nightmare!")
					end
				end,
			},
			{
				text = "Give a safe, cliche answer",
				effects = { Smarts = 2 },
				feedText = "Just trying to win games...",
				onResolve = function(state)
					state:AddFeed("🎤 Boring but safe. Nobody can quote you. Smart move.")
				end,
			},
			{
				text = "Walk out of the interview",
				effects = { Happiness = -3 },
				feedText = "Not dealing with this...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 50 then
						state.Fame = (state.Fame or 0) + 10
						state:AddFeed("🎤 The walkout became a meme! Actually kind of cool now!")
					else
						state:ModifyStat("Happiness", -8)
						state:AddFeed("🎤 Made you look bad. Media calling you 'difficult'.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_slump",
		title = "📉 Performance Slump",
		emoji = "📉",
		text = "You've been playing terribly for weeks. Fans are booing. Coach is benching you. The confidence is gone.",
		category = "career_sports",
		weight = 14,
		minAge = 18, maxAge = 40,
		baseChance = 0.5,
		cooldown = 4, -- CRITICAL FIX: Increased from 2 to reduce spam
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Work harder - extra practice",
				effects = { Health = -5, Happiness = -3 },
				feedText = "Grind through this...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 60 then
						state:ModifyStat("Happiness", 18)
						state.Money = (state.Money or 0) + math.random(50000, 200000)
						state:AddFeed("📉 BROKE OUT OF IT! Big game performance! Redemption story!")
					else
						state:ModifyStat("Happiness", -8)
						state:ModifyStat("Health", -5)
						state:AddFeed("📉 Overworked yourself. Now injured too. Should've rested.")
					end
				end,
			},
			{
				text = "See a sports psychologist ($3,000)",
				effects = { Money = -3000, Smarts = 3 },
				feedText = "It's mental...",
				-- CRITICAL FIX #7: Add eligibility check!
				eligibility = function(state) return (state.Money or 0) >= 3000, "💸 Need $3,000 for psychologist" end,
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 70 then
						state:ModifyStat("Happiness", 15)
						state.Flags = state.Flags or {}
						state.Flags.mentally_tough = true
						state:AddFeed("📉 Mind right again! The mental coaching worked wonders!")
					else
						state:AddFeed("📉 Helped a bit. Still working through it.")
					end
				end,
			},
			{
				text = "Party and forget about it",
				effects = { Happiness = 5, Health = -3 },
				feedText = "Need to clear my head...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						state:ModifyStat("Happiness", 10)
						state:AddFeed("📉 Actually helped! Came back refreshed and played great!")
					else
						state:ModifyStat("Happiness", -15)
						state:AddFeed("📉 Partying made it worse. Coach is furious. Bad look.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_charity_game",
		title = "❤️ Charity Event Invite",
		emoji = "❤️",
		text = "You've been invited to participate in a celebrity charity game. Great PR, but takes time during your offseason.",
		category = "career_sports",
		weight = 12,
		minAge = 18, maxAge = 50,
		baseChance = 0.45,
		cooldown = 4, -- CRITICAL FIX: Increased from 2 to reduce spam
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Happily participate",
				effects = { Happiness = 12 },
				feedText = "For a good cause!",
				onResolve = function(state)
					state.Fame = (state.Fame or 0) + 15
					state.Flags = state.Flags or {}
					state.Flags.charitable_athlete = true
					state:AddFeed("❤️ Great event! Made some kids' dreams come true! Wholesome content!")
				end,
			},
		{
			text = "Go all out - donate and organize ($25K)",
			effects = { Happiness = 15, Money = -25000 },
			feedText = "Let's make it special!",
			eligibility = function(state) return (state.Money or 0) >= 25000, "💸 Need $25K for charity event" end,
			onResolve = function(state)
					state.Fame = (state.Fame or 0) + 35
					state.Flags = state.Flags or {}
					state.Flags.community_hero = true
					state:AddFeed("❤️ You went above and beyond! Community loves you! Story went national!")
				end,
			},
			{
				text = "Decline - need to rest",
				effects = { Health = 5 },
				feedText = "Body needs recovery time.",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 50 then
						state:AddFeed("❤️ Understood. Used the time to recover well.")
					else
						state:ModifyStat("Happiness", -8)
						state:AddFeed("❤️ People noticed you skipped. 'Too good for charity?' social media asks.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_team_chemistry",
		title = "🤝 Locker Room Drama",
		emoji = "🤝",
		text = "There's tension between two star players. The team is divided. As a respected voice, everyone's looking to you.",
		category = "career_sports",
		weight = 12,
		minAge = 22, maxAge = 40,
		baseChance = 0.45,
		cooldown = 3,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true, team_outcast = true },
		
		choices = {
			{
				text = "Mediate and bring them together",
				effects = { Smarts = 3 },
				feedText = "Team first...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 55 then
						state:ModifyStat("Happiness", 15)
						state.Flags = state.Flags or {}
						state.Flags.team_leader = true
						state:AddFeed("🤝 You brought them together! Team chemistry is PERFECT now! Leader vibes!")
					else
						state:ModifyStat("Happiness", -5)
						state:AddFeed("🤝 Tried your best but they're still beefing. Can't win them all.")
					end
				end,
			},
			{
				text = "Side with one player",
				effects = {},
				feedText = "Pick a side...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						state:ModifyStat("Happiness", 10)
						state:AddFeed("🤝 Your guy won the power struggle! You're in the inner circle!")
					else
						state:ModifyStat("Happiness", -10)
						state:AddFeed("🤝 Wrong side. Now you've made an enemy on your own team.")
					end
				end,
			},
			{
				text = "Stay out of it",
				effects = { Happiness = 2 },
				feedText = "Not my problem...",
				onResolve = function(state)
					state:AddFeed("🤝 Smart move. Let them figure it out themselves.")
				end,
			},
		},
	},
	
	{
		id = "athlete_all_star",
		title = "⭐ All-Star Selection!",
		emoji = "⭐",
		text = "You've been selected for the All-Star game! The best of the best! What an honor!",
		category = "career_sports",
		weight = 12,
		minAge = 20, maxAge = 38,
		baseChance = 0.4,
		cooldown = 3,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true, injured = true },
		
		choices = {
			{
				text = "Play and try to win MVP",
				effects = { Health = -3 },
				feedText = "Give the fans a show!",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 30 then
						local bonus = math.random(100000, 500000)
						state.Money = (state.Money or 0) + bonus
						state.Fame = (state.Fame or 0) + 40
						state:ModifyStat("Happiness", 25)
						state.Flags = state.Flags or {}
						state.Flags.all_star_mvp = true
						state:AddFeed(string.format("⭐ ALL-STAR MVP! $%d bonus! Best player in the game!", bonus))
					else
						state.Fame = (state.Fame or 0) + 20
						state:ModifyStat("Happiness", 12)
						state:AddFeed("⭐ Great showing! Held your own among the elite!")
					end
				end,
			},
			{
				text = "Enjoy the experience",
				effects = { Happiness = 10 },
				feedText = "Soak it in...",
				onResolve = function(state)
					state.Fame = (state.Fame or 0) + 15
					state:AddFeed("⭐ Amazing experience! Met legends! Made memories!")
				end,
			},
			{
				text = "Skip due to minor injury",
				effects = { Health = 5 },
				feedText = "Not worth the risk...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 50 then
						state:AddFeed("⭐ Good call. Your body needed the rest for the stretch run.")
					else
						state:ModifyStat("Happiness", -8)
						state:AddFeed("⭐ Fans disappointed. Replacement player balled out. FOMO hitting.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_social_media_beef",
		title = "📱 Social Media War",
		emoji = "📱",
		text = "A rival player called you out on social media! They're talking trash and it's going viral! How do you respond?",
		category = "career_sports",
		weight = 14,
		minAge = 18, maxAge = 40,
		baseChance = 0.5,
		cooldown = 4, -- CRITICAL FIX: Increased from 2 to reduce spam
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Fire back with a brutal clapback",
				effects = { Happiness = 5 },
				feedText = "Let's go...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 50 then
						state.Fame = (state.Fame or 0) + 20
						state:ModifyStat("Happiness", 15)
						state:AddFeed("📱 YOUR RESPONSE WON! W! Everyone's on your side! Ratio'd them!")
					else
						state.Fame = (state.Fame or 0) + 5
						state:ModifyStat("Happiness", -5)
						state:AddFeed("📱 Twitter war continues. Both of you look silly now.")
					end
				end,
			},
			{
				text = "Let your play do the talking",
				effects = { Smarts = 3 },
				feedText = "I'll see you on the field/court.",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 60 then
						state.Fame = (state.Fame or 0) + 25
						state:ModifyStat("Happiness", 18)
						state:AddFeed("📱 Dominated them when you played! Actions > Words! Classy response!")
					else
						state:ModifyStat("Happiness", -8)
						state:AddFeed("📱 They outplayed you. Their trash talk was right. Tough L.")
					end
				end,
			},
			{
				text = "Ignore it completely",
				effects = { Smarts = 2 },
				feedText = "Won't even acknowledge it.",
				onResolve = function(state)
					state:AddFeed("📱 Took the high road. Respect from the media. Mature look.")
				end,
			},
		},
	},
	
	{
		id = "athlete_record_chase",
		title = "📊 Chasing a Record!",
		emoji = "📊",
		text = "You're on pace to break an all-time record! The pressure is immense! Media is following your every move!",
		category = "career_sports",
		weight = 10,
		minAge = 22, maxAge = 38,
		baseChance = 0.35,
		cooldown = 4,
		requiresJob = true,
		eligibility = isProAthlete,
		blockedByFlags = { in_prison = true, injured = true },
		
		choices = {
			{
				text = "Embrace the pressure - make history!",
				effects = { Happiness = 5, Health = -5 },
				feedText = "Born for this!",
				triggerMinigame = "mash",
				minigameOptions = { difficulty = "hard" },
				onResolve = function(state, minigameResult)
					local roll = math.random(1, 100)
					local bonus = (minigameResult and minigameResult.success) and 25 or 0
					
					if roll + bonus >= 50 then
						local bonus_money = math.random(1000000, 5000000)
						state.Money = (state.Money or 0) + bonus_money
						state.Fame = math.min(100, (state.Fame or 0) + 80) -- CRITICAL FIX: Cap fame at 100
						state:ModifyStat("Happiness", 30)
						state.Flags = state.Flags or {}
						state.Flags.record_holder = true
						state:AddFeed(string.format("📊 RECORD BROKEN! YOUR NAME IN HISTORY FOREVER! $%d in bonuses!", bonus_money))
					else
						state.Fame = (state.Fame or 0) + 15
						state:ModifyStat("Happiness", -10)
						state:AddFeed("📊 Fell just short! Still an amazing season but the record stands.")
					end
				end,
			},
			{
				text = "Block out the noise - focus",
				effects = { Smarts = 3 },
				feedText = "One game at a time...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						local bonus_money = math.random(500000, 2000000)
						state.Money = (state.Money or 0) + bonus_money
						state.Fame = math.min(100, (state.Fame or 0) + 60) -- CRITICAL FIX: Cap fame at 100
						state:ModifyStat("Happiness", 25)
						state.Flags = state.Flags or {}
						state.Flags.record_holder = true
						state:AddFeed(string.format("📊 QUIETLY MADE HISTORY! Record is yours! $%d bonus!", bonus_money))
					else
						state:ModifyStat("Happiness", 5)
						state:AddFeed("📊 Solid season regardless. The record chase was fun!")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_coach_conflict",
		title = "😡 Conflict with Coach",
		emoji = "😡",
		text = "Your coach publicly criticized you after a loss. The comments were harsh and felt personal. What do you do?",
		category = "career_sports",
		weight = 12,
		minAge = 18, maxAge = 40,
		baseChance = 0.45,
		cooldown = 4, -- CRITICAL FIX: Increased from 2 to reduce spam
		requiresJob = true,
		eligibility = hasAthleteJob,
		blockedByFlags = { in_prison = true },
		
		choices = {
			{
				text = "Demand a meeting - clear the air",
				effects = { Smarts = 2 },
				feedText = "We need to talk...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 55 then
						state:ModifyStat("Happiness", 12)
						state:AddFeed("😡 Cleared the air! Coach apologized. Respect earned!")
					else
						state:ModifyStat("Happiness", -10)
						state:AddFeed("😡 Meeting got heated. Relationship might be damaged.")
					end
				end,
			},
			{
				text = "Respond through media",
				effects = {},
				feedText = "Let the public know my side...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 35 then
						state.Fame = (state.Fame or 0) + 15
						state:ModifyStat("Happiness", 10)
						state:AddFeed("😡 Public sided with you! Coach looks bad!")
					else
						state:ModifyStat("Happiness", -15)
						state.Flags = state.Flags or {}
						state.Flags.coach_enemy = true
						state:AddFeed("😡 Now you're both in a media war. Trade rumors starting...")
					end
				end,
			},
			{
				text = "Accept it and move on",
				effects = { Happiness = -5, Smarts = 3 },
				feedText = "He's right, need to be better.",
				onResolve = function(state)
					state:ModifyStat("Happiness", 8)
					state.Flags = state.Flags or {}
					state.Flags.coachable = true
					state:AddFeed("😡 Took the criticism like a pro. Earned respect from teammates.")
				end,
			},
		},
	},
	
	{
		id = "athlete_business_venture",
		title = "💼 Business Opportunity",
		emoji = "💼",
		text = "A businessman approaches you about investing in a new venture. Could be huge returns or a total loss.",
		category = "career_sports",
		weight = 12,
		minAge = 22, maxAge = 50,
		baseChance = 0.45,
		cooldown = 3,
		requiresJob = true,
		eligibility = function(state)
			return hasAthleteJob(state) and (state.Money or 0) >= 100000
		end,
		blockedByFlags = { in_prison = true },
		
	choices = {
		{
			text = "Invest big ($100,000)",
			effects = {},
			feedText = "High risk, high reward!",
			-- CRITICAL FIX: Add per-choice eligibility check
			eligibility = function(state) return (state.Money or 0) >= 100000, "💸 Need $100,000 to invest" end,
			onResolve = function(state)
				-- CRITICAL FIX: Prevent negative money
				state.Money = math.max(0, (state.Money or 0) - 100000)
				local roll = math.random(1, 100)
				if roll <= 30 then
					local returns = math.random(500000, 2000000)
					state.Money = (state.Money or 0) + returns
					state:ModifyStat("Happiness", 25)
					state.Flags = state.Flags or {}
					state.Flags.smart_investor = true
					state:AddFeed(string.format("💼 JACKPOT! Investment returned $%d! Business genius!", returns))
				elseif roll <= 60 then
					local returns = math.random(80000, 150000)
					state.Money = (state.Money or 0) + returns
					state:ModifyStat("Happiness", 5)
					state:AddFeed(string.format("💼 Decent returns. Got $%d back. Not bad!", returns))
				else
					state:ModifyStat("Happiness", -20)
					state:AddFeed("💼 SCAM! Lost everything! Expensive lesson about business...")
				end
			end,
		},
			{
				text = "Pass - stick to what you know",
				effects = { Smarts = 2 },
				feedText = "I'm an athlete, not a businessman.",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 40 then
						state:ModifyStat("Happiness", -5)
						state:AddFeed("💼 That business blew up huge. Massive FOMO...")
					else
						state:AddFeed("💼 Good call. Many athletes go broke from bad investments.")
					end
				end,
			},
		},
	},
	
	{
		id = "athlete_comeback_story",
		title = "💪 Comeback Season",
		emoji = "💪",
		text = "After struggling with injuries, you're finally healthy. This is your chance to prove you're still elite!",
		category = "career_sports",
		weight = 12,
		minAge = 25, maxAge = 40,
		baseChance = 0.45,
		cooldown = 3,
		eligibility = function(state)
			return hasAthleteJob(state) and state.Flags and 
				   (state.Flags.minor_injury or state.Flags.major_injury)
		end,
		blockedByFlags = { in_prison = true, injured = true },
		
		choices = {
			{
				text = "Go all out - nothing to lose",
				effects = { Health = -5, Happiness = 8 },
				feedText = "Redemption time!",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 50 then
						local bonus = math.random(500000, 2000000)
						state.Money = (state.Money or 0) + bonus
						state.Fame = (state.Fame or 0) + 40
						state:ModifyStat("Happiness", 25)
						state.Flags = state.Flags or {}
						state.Flags.comeback_player = true
						state.Flags.minor_injury = nil
						state.Flags.major_injury = nil
						state:AddFeed(string.format("💪 COMEBACK PLAYER OF THE YEAR! $%d bonus! Inspirational story!", bonus))
					else
						state:ModifyStat("Happiness", 5)
						state:AddFeed("💪 Decent season. Proved you can still play. Not elite but solid.")
					end
				end,
			},
			{
				text = "Pace yourself - long season",
				effects = { Health = 3, Smarts = 2 },
				feedText = "Smart and steady...",
				onResolve = function(state)
					local roll = math.random(1, 100)
					if roll <= 60 then
						state:ModifyStat("Happiness", 15)
						state.Flags = state.Flags or {}
						state.Flags.minor_injury = nil
						state.Flags.major_injury = nil
						state:AddFeed("💪 Played the whole season healthy! That's the real win!")
					else
						state:AddFeed("💪 Stayed healthy but stats were pedestrian. Critics whispering 'washed'.")
					end
				end,
			},
		},
	},
}

return AthleteCareerEvents
